# VitaMend TODOs

## High Priority
- [ ] Implement Levenshtein distance for duplicate checking in `lib/ai-verification-engine.ts`.
- [ ] Add actual dashboard metrics to `app/(public)/page.tsx`.
- [ ] Fix intermittent Gemini API timeouts when processing blurry images.
- [ ] Add rate limiting to the main `/api/donate` route.
- [ ] Secure the NextAuth v5 session callbacks properly for role-based access.

## Medium Priority
- [ ] Improve mobile responsiveness for the donation flow.
- [ ] Create a cron job to auto-archive expired medicines.
- [ ] Integrate a real email service provider for notifications (currently using console.log).
